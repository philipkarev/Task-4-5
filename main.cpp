#include "tokenaizer.h"

int main() {
    /*int i = 1;
    char s[] = "Hello, Wor,    0909()-=l,d!";
    char *str = strtok(s, ",");
    cout << "\n" << str << "\n";
    str = strtok(NULL, ",");
    cout << str << "\n";
    str = strtok(NULL, ",");
    cout << str << "\n";
    str = strtok(NULL, ",");
    cout << str << "\n";*/

    //char str1[] = "Hello, World!"; cout << strlen(str1) << endl; cout << str1[12] << endl;

    //char str2[3]; str2[0] = 1; str2[1] = 2; cout << str2[2] << endl;

    //tokenaizer obj = tokenaizer(str1, strlen(str1)+1);
    //cout << obj;

    /*string path = "delim.txt", path2 = "1.txt";

    ifstream fin;
    fin.open(path);

    if (!fin.is_open()) {
        cout << "error: could not open the file" << endl;
    } else {
        cout << "file is open" << endl;
        char ch;
        while (fin.get(ch))
            cout << ch;
    }

    fin.close();*/

    return 0;
}
